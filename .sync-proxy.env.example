# ============================================================
# sync-vault.sh 代理配置文件（本文件不会提交到仓库，见 .gitignore）
#
# 用法：
#   1. 复制本文件为 .sync-proxy.env：  cp .sync-proxy.env.example .sync-proxy.env
#   2. 按需填写代理地址（支持两种写法）：
#       写法A：只提供一个地址，http 和 https 都使用它
#         HTTP_PROXY=http://127.0.0.1:7890
#       写法B：分别指定
#         HTTP_PROXY=http://127.0.0.1:7890
#         HTTPS_PROXY=http://127.0.0.1:7890
#   3. 不需要代理时：将下面所有行删除或注释掉即可，sync 脚本会自动直连。
#
# 也可设置忽略代理的地址（可选）：
#   NO_PROXY=localhost,127.0.0.1,.github.com
# ============================================================
